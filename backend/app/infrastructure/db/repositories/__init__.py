"""Concrete SQLAlchemy repositories implementing the domain repository ports.

Each repository translates between domain entities and ORM models, owning all
SQL/ORM detail so the rest of the system never sees it. Populated from Phase 2.
"""
